<h1>Create a New Product</h1>

<%= form_tag("/products", method: "post") do %>
  <%= render 'form' %>
  <%= submit_tag("Create Product") %>
<% end %>

<!-- <% if @products.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@products.errors.count, "error") %> prohibited this product from being created, please try again:</h2>

    <ul>
    <% @products.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %> -->


  <!-- <div>
    <%= label_tag(:name) %>
    <%= text_field_tag(:name, @products.name) %>
  </div>

  <div>
    <%= label_tag(:price) %>
    <%= text_field_tag(:price, @products.price) %>
  </div>

  <div>
    <%= label_tag(:image) %>
    <%= text_field_tag(:image, @products.image) %>
  </div>

  <div>
    <%= label_tag(:description) %>
    <%= text_field_tag(:description, @products.description) %>
  </div>

  <div> -->
